<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ssm.manager.dao.UserLoginDao">
    <resultMap id="UserLoginBase" type="com.ssm.manager.pojo.UserLogin">
        <id column="USER_ID" property="userId"/>
        <result column="USER_NAME" property="userName"/>
        <result column="USER_PASSWORD" property="userPassword"/>
        <result column="SALT" property="salt"/>
        <result column="IS_LOCK" property="isLock"/>
        <result column="GMT_CREATE" property="gmtCreate"/>
        <result column="GMT_UPDATE" property="gmtUpdate"/>
    </resultMap>

    <select id="select" parameterType="long" resultMap="UserLoginBase">
        SELECT * FROM user_login WHERE USER_ID=#{userId}
    </select>

    <insert id="insert" parameterType="com.ssm.manager.pojo.UserLogin" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user_login
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userName != null">
                USER_NAME,
            </if>
            <if test="userPassword != null">
                USER_PASSWORD,
            </if>
            <if test="salt != null">
                SALT,
            </if>
        </trim>
        <trim prefix="values(" suffix=")" suffixOverrides=",">
            <if test="userName != null">
                #{userName},
            </if>
            <if test="userPassword != null">
                #{userPassword},
            </if>
            <if test="salt != null">
                #{salt},
            </if>
        </trim>
    </insert>

    <select id="selectByUserName" parameterType="string" resultMap="UserLoginBase">
        SELECT * FROM user_login WHERE USER_NAME=#{userName}
    </select>
</mapper>